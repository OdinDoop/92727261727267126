# FB State Messenger Bot

A Messenger bot that uses Facebook session cookies from `account.txt` to send messages.

## Setup

1. **Clone repository**
```bash
git clone <your-repo>
cd messenger-test-bot